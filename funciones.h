#include "funciones.c" //Inclucion de libreria de funciones

float suma(float x, float y); //Dando un valor a "x" e "y" la funcion devuelve como resultado la suma de las variables (x*y)

float resta(float x, float y); //Dando un valor a "x" e "y" la funcion devuelve como resultado la resta de las variables (x-y)

float division(float x, float y); //Dando un valor a "x" e "y" la funcion devuelve como resultado la division de las variables (x/y)

float multiplicacion (float x, float y); //Dando un valor a "x" e "y" la funcion devuelve como resultado la multiplicacion de las variables (x*y)

int factorial (int x); //Dando un valor a "x" la funcion devuelve como resultado el factorial de la variable (!x)
