#include "funciones.c" //Inclucion de libreria de funciones

float suma(float x, float y); //Toma dos valores y devuelve la suma de los mismos

float resta(float x, float y); //Toma dos valores y devuelve la resta de los mismos

float division(float x, float y); //Toma dos valores y devuelve la division de (x/y)

float multiplicacion (float x, float y); //Dando un valor a "x" e "y" la funcion devuelve como resultado la multiplicacion de las variables (x*y)

int factorial (int x); //Tomando "x" calcula su factorial. POSITIVO, NEGATIVO y Validacion (!x)
